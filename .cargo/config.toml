[build]
# Build configuration

# NOTE: Clippy lints are configured in Cargo.toml [lints] section
# This keeps configuration centralized and allows different settings
# for lib vs tests via #![cfg_attr(test, allow(...))] in lib.rs
